# 模块七：系统集成与测试说明书

## 📋 模块概述

### 模块名称
系统集成与测试（System Integration & Testing）

### 模块定位
负责各业务模块的集成、系统测试、性能优化和部署上线。

### 开发优先级
**P0（最高优先级）** - 在所有模块开发完成后进行

---

## 🎯 模块目标

### 主要目标
1. 完成各模块的集成联调
2. 进行全面的系统测试
3. 性能优化与调优
4. 部署上线准备
5. 编写部署文档

---

## 🔗 模块集成

### 集成架构
```
┌─────────────────────────────────────────────────────────────┐
│                        前端集成                             │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐ │
│  │ 客户模块  │ 商机模块  │ 行为模块  │ AI模块   │ 分析模块 │ │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘ │
│                            ↓                                │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              前端路由与状态管理                        │  │
│  │              API请求统一封装                          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                        后端集成                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API网关 / Nginx                          │  │
│  └──────────────────────────────────────────────────────┘  │
│                            ↓                                │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐ │
│  │ 认证服务  │ 客户服务  │ 商机服务  │ 行为服务  │ AI服务  │ │
│  │          │          │          │          │ 分析服务│ │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘ │
│                            ↓                                │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              共享服务层                               │  │
│  │    - 日志服务 - 缓存服务 - 消息队列 - 文件存储       │  │
│  └──────────────────────────────────────────────────────┘  │
│                            ↓                                │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              数据库层 (PostgreSQL + Redis)           │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 集成检查清单

#### 1. API接口集成
- [ ] 所有API接口已定义并实现
- [ ] 接口文档完整（Swagger/OpenAPI）
- [ ] 接口版本控制（/api/v1）
- [ ] 接口统一响应格式
- [ ] 错误码规范统一
- [ ] 跨域配置正确

#### 2. 认证授权集成
- [ ] JWT认证流程完整
- [ ] Token刷新机制正常
- [ ] 权限控制应用到所有接口
- [ ] 前端路由守卫配置
- [ ] 页面权限控制生效

#### 3. 数据流集成
- [ ] 客户→商机数据关联
- [ ] 商机→联系人数据关联
- [ ] 商机→活动数据关联
- [ ] AI模块与各模块数据交互
- [ ] 统计数据聚合正确

#### 4. 前端集成
- [ ] 路由配置完整
- [ ] 状态管理配置
- [ ] API请求封装
- [ ] 公共组件复用
- [ ] 样式主题统一

---

## 🧪 测试计划

### 测试类型

#### 1. 单元测试
```python
# 测试框架: pytest
# 目标覆盖率: 80%+

# 示例: 客户服务测试
class TestCustomerService:
    def test_create_customer(self):
        customer_data = {...}
        result = customer_service.create(customer_data)
        assert result.id is not None
        assert result.customer_name == customer_data['customer_name']

    def test_create_duplicate_customer(self):
        # 测试重复创建
        with pytest.raises(DuplicateError):
            customer_service.create(duplicate_data)
```

#### 2. 接口测试
```python
# 测试框架: pytest + httpx
async def test_create_customer_api():
    async with httpx.AsyncClient(app=app, base_url="http://test") as client:
        response = await client.post(
            "/api/v1/customers",
            json={...},
            headers={"Authorization": "Bearer token"}
        )
        assert response.status_code == 201
        assert response.json()["data"]["customer_name"] == "..."
```

#### 3. 集成测试
```python
# 测试端到端流程
async def test_opportunity_lifecycle():
    # 1. 创建客户
    customer = await create_customer(...)

    # 2. 创建商机
    opportunity = await create_opportunity(customer_id=customer.id)

    # 3. 关联联系人
    await add_contact_to_opportunity(...)

    # 4. 推进阶段
    await advance_stage(opportunity.id, new_stage_id)

    # 5. 标记赢单
    await mark_won(opportunity.id)

    # 验证结果
    final_opportunity = await get_opportunity(opportunity.id)
    assert final_opportunity.status == "won"
```

#### 4. 性能测试
```python
# 测试工具: locust
class CRMUser(HttpUser):
    wait_time = between(1, 3)

    def on_start(self):
        # 登录
        response = self.client.post("/api/v1/auth/login", json={...})
        self.token = response.json()["data"]["access_token"]

    @task(3)
    def view_customer_list(self):
        self.client.get(
            "/api/v1/customers",
            headers={"Authorization": f"Bearer {self.token}"}
        )

    @task(2)
    def view_opportunity_list(self):
        self.client.get("/api/v1/opportunities", ...)
```

#### 5. 安全测试
```bash
# SQL注入测试
sqlmap -u "http://localhost:8000/api/v1/customers?id=1" \
       --headers="Authorization: Bearer token"

# XSS测试
# 在所有输入框测试 <script>alert(1)</script>

# 权限绕过测试
# 尝试无token访问、过期token访问、低权限用户访问高权限接口
```

### 测试用例清单

#### 客户模块测试
| 测试项 | 测试场景 | 预期结果 |
|-------|---------|---------|
| 创建客户 | 正常创建 | 返回客户ID |
| | 重复名称 | 提示错误 |
| | 必填字段缺失 | 返回400错误 |
| 更新客户 | 正常更新 | 更新成功 |
| | 更新他人客户 | 返回403错误 |
| 删除客户 | 软删除 | 状态变为deleted |
| | 有关联商机 | 提示无法删除 |
| 查询客户 | 列表分页 | 返回正确分页 |
| | 条件筛选 | 返回筛选结果 |
| 转移客户 | 正常转移 | 负责人变更 |

#### 商机模块测试
| 测试项 | 测试场景 | 预期结果 |
|-------|---------|---------|
| 创建商机 | 正常创建 | 返回商机ID |
| | 客户不存在 | 返回400错误 |
| 阶段推进 | 正常推进 | 阶段变更 |
| | 跳跃阶段 | 根据配置允许/拒绝 |
| | 重复推进到同一阶段 | 提示错误 |
| 标记赢单 | 正常赢单 | 状态变为won |
| | 已关闭商机 | 提示错误 |
| 标记输单 | 正常输单 | 状态变为lost |
| | 需要输单原因 | 要求填写原因 |

#### AI模块测试
| 测试项 | 测试场景 | 预期结果 |
|-------|---------|---------|
| 信息补全 | 正常请求 | 返回补全信息 |
| | API Key无效 | 返回错误 |
| | 并发请求 | 限流控制 |
| 风险分析 | 正常分析 | 返回风险等级 |
| | 无数据 | 返回提示 |
| 自然查询 | 正常查询 | 返回结果 |
| | 无法理解 | 返回默认建议 |

---

## 🚀 部署方案

### 部署架构
```
┌─────────────────────────────────────────────────────────────┐
│                        生产环境                             │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              CDN / 负载均衡                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                            ↓                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Nginx (反向代理 + SSL)                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                            ↓                                │
│  ┌──────────────────────┬──────────────────────────────┐   │
│  │    前端 (Static)     │     后端 (FastAPI)           │   │
│  │  Vue3 Build Files    │  Docker Container x N        │   │
│  └──────────────────────┴──────────────────────────────┘   │
│                              ↓                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              PostgreSQL (主从复制)                   │   │
│  │              Redis (主从 + 哨兵)                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              监控告警 (Prometheus + Grafana)         │   │
│  │              日志收集 (ELK/Loki)                     │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### Docker Compose 配置
```yaml
version: '3.8'

services:
  # 前端
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  # 后端
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:pass@db:5432/crm
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

  # 数据库
  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=crm
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Redis
  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

  # Celery Worker (异步任务)
  celery_worker:
    build: ./backend
    command: celery -A app.celery worker --loglevel=info
    depends_on:
      - redis
      - db

volumes:
  postgres_data:
  redis_data:
```

### 部署步骤

#### 1. 环境准备
```bash
# 1. 安装Docker和Docker Compose
curl -fsSL https://get.docker.com | sh
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# 2. 创建项目目录
mkdir -p /opt/crm-system
cd /opt/crm-system

# 3. 克隆代码
git clone https://github.com/xxx/crm-system.git .
```

#### 2. 配置环境变量
```bash
# 创建 .env 文件
cat > .env << EOF
# 数据库
DATABASE_URL=postgresql://crm_user:secure_password@db:5432/crm_db

# Redis
REDIS_URL=redis://:redis_password@redis:6379/0

# JWT
JWT_SECRET_KEY=your-very-secure-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# AI
LLM_PROVIDER=glm
LLM_GLM_API_KEY=your-glm-api-key

# 应用
APP_NAME=CRM System
APP_VERSION=1.0.0
DEBUG=false
ALLOWED_ORIGINS=https://your-domain.com
EOF
```

#### 3. 构建和启动
```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 初始化数据库
docker-compose exec backend alembic upgrade head

# 创建初始数据
docker-compose exec backend python scripts/init_data.py

# 检查服务状态
docker-compose ps
docker-compose logs -f
```

#### 4. 配置Nginx
```nginx
# /etc/nginx/sites-available/crm
upstream backend {
    server localhost:8000;
}

server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /opt/crm-system/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端API
    location /api {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket (如果需要)
    location /ws {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

#### 5. 配置SSL (Let's Encrypt)
```bash
# 安装certbot
apt-get install certbot python3-certbot-nginx

# 获取证书
certbot --nginx -d your-domain.com

# 自动续期
certbot renew --dry-run
```

---

## 📊 监控与日志

### 监控指标
| 类别 | 指标 | 告警阈值 |
|-----|------|---------|
| 系统 | CPU使用率 | > 80% |
| | 内存使用率 | > 85% |
| | 磁盘使用率 | > 90% |
| 应用 | API响应时间 | > 1s |
| | 错误率 | > 5% |
| | 请求QPS | < 100 |
| 数据库 | 连接数 | > 80% |
| | 慢查询 | > 1s |
| | 死锁 | 任何 |

### 日志配置
```python
# 日志配置
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'standard': {
            'format': '%(asctime)s [%(levelname)s] %(name)s: %(message)s'
        },
    },
    'handlers': {
        'file': {
            'class': 'logging.handlers.RotatingFileHandler',
            'filename': '/var/log/crm/app.log',
            'maxBytes': 10485760,  # 10MB
            'backupCount': 10,
            'formatter': 'standard',
        },
    },
    'loggers': {
        '': {
            'handlers': ['file'],
            'level': 'INFO',
        },
    }
}
```

---

## 📋 集成测试任务清单

### 集成阶段
- [ ] API接口集成测试
- [ ] 前后端联调
- [ ] 数据流验证
- [ ] 第三方服务集成测试

### 功能测试
- [ ] 用户管理功能测试
- [ ] 客户管理功能测试
- [ ] 商机管理功能测试
- [ ] 销售行为管理测试
- [ ] AI功能测试
- [ ] 数据分析测试
- [ ] 报表功能测试

### 性能测试
- [ ] 接口性能测试
- [ ] 并发压力测试
- [ ] 数据库性能测试
- [ ] 前端加载性能测试

### 安全测试
- [ ] 认证授权测试
- [ ] SQL注入测试
- [ ] XSS测试
- [ ] CSRF测试
- [ ] 文件上传测试

### 兼容性测试
- [ ] 浏览器兼容性
- [ ] 移动端适配
- [ ] 不同分辨率测试

### 部署准备
- [ ] 生产环境配置
- [ ] 数据库迁移脚本
- [ ] 备份恢复方案
- [ ] 监控告警配置
- [ ] 部署文档编写

---

## ✅ 验收标准

### 功能验收
- [ ] 所有功能正常工作
- [ ] 模块间数据正确流转
- [ ] 无严重Bug

### 性能验收
- [ ] 页面加载 < 2秒
- [ ] API响应 < 500ms
- [ ] 支持100+并发

### 安全验收
- [ ] 通过安全扫描
- [ ] 敏感数据加密
- [ ] 权限控制完整

---

## 📚 上线清单

### 上线前检查
- [ ] 所有测试通过
- [ ] 性能达标
- [ ] 安全扫描通过
- [ ] 备份方案就绪
- [ ] 回滚方案准备

### 上线步骤
1. 数据库备份
2. 代码部署
3. 数据库迁移
4. 配置更新
5. 服务重启
6. 健康检查
7. 验证核心功能

### 上线后监控
- [ ] 系统状态监控
- [ ] 错误日志监控
- [ ] 用户反馈收集
- [ ] 性能指标监控

---

*模块负责人：待分配*
*预计工期：1周*
*依赖模块：所有业务模块*
*被依赖模块：无*

---

*文档版本：v1.0*
*创建日期：2026-02-26*
